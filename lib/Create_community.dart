import 'package:flutter/material.dart';

class Create_community extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text("Create community page", style: Theme.of(context).textTheme.titleLarge,),
    );
  }
}