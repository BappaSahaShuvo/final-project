import 'package:flutter/material.dart';
class Channel_invites extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text("Channel invites page", style: Theme.of(context).textTheme.titleLarge,),
    );
  }
}