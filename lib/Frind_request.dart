import 'package:flutter/material.dart';

class Frind_request extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text("Frind request page", style: Theme.of(context).textTheme.titleLarge,),
    );
  }
}