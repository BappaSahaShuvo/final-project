import 'package:flutter/material.dart';

class Message_requests extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text("Message requests Page", style: Theme.of(context).textTheme.titleLarge,),
    );
  }
}