import 'package:flutter/material.dart';

class Archive extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text("Archive page", style: Theme.of(context).textTheme.titleLarge,),
    );
  }
}